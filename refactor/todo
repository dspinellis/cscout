$Id: todo,v 1.40 2003/08/21 20:57:44 dds Exp $

In the query change check boxes into yes/no/don't care radio buttos
Option to specify leading file path to hide

Function information
	New identifier attribute is_function
	Per file: Defined functions
	Per function:
		Define in file href line href
		Functions that call it
		All functions that call it
		Functions it calls
		All functions it calls
		Note: function calls through pointers are not included
		in the call graph.
	Functions
		Function metrics
		All functions
		Project-scoped writable functions
		File-scoped writable functions
		Call graph 
		Call graph in GraphViz dot format
		Function query
			Writable
			Read-only
			Project scope
			File scope
			Name should match RE
			Name of functions that call it should match RE
			Name of functions it calls should match RE
			Select functions from filenames matching RE
			Query title


Borland configuration file
Solaris compilation
experiment with hashmap performance

ceescape (swill) front end:

Allow interruptions in id queries

lex preprocessor / hooks
#pragma sync "filename" offset
#pragma nosync (default. Input is parsed, but unify is a null op)

C99 extensions

Convert color to cgi/command-line

Convert the _asm handling into generic handlers for excluding line and
block-based compiler extensions via compiler-specific preprocess-specific preprocessor defs.

Create Java-based front end
Make color conform with the other programs

Add isfunction identifier property
Add ispointer identifier property

Create separate database table with the newlines

Optimize (hash for map)
Create editor
